# 正式实现验收矩阵

本矩阵用于区分自动验证、微信工具验证、真机验证和必须由项目所有者完成的生产联调。代码存在但没有相应证据，不算完成对应验收。

| 领域 | 验收行为 | 自动测试 | 微信工具 | 真机/生产 |
|---|---|---:|---:|---:|
| 规则 | 2-5 人初始化、56 张牌、牌数守恒 | 必须 | - | - |
| 规则 | 全部基础牌、组合、Nope、Attack、Defuse | 必须 | - | - |
| 隐私 | 对手牌面、牌堆顺序、私密选择不进入玩家视图 | 必须 | - | 抽查 |
| 协议 | 非法 schema、actorId、客户端时间、系统命令被拒绝 | 必须 | - | - |
| 传输认证 | `callContainer` 登录后由 `connectContainer` 建连；首个 `resume.resumeToken` 携带 Bearer token，URL 不包含会话凭证 | 必须 | 调试 | iOS + Android |
| 幂等 | 相同 commandId 返回原结果，复用不同 payload 被拒绝 | 必须 | - | 弱网复核 |
| 幂等 | 房间业务提交与通用会话回执之间的崩溃窗口 | 部分；已记录 | - | 故障演练 |
| 同步 | 全量快照 revision 可跳跃，旧快照不覆盖新状态 | 必须 | - | 弱网复核 |
| 恢复 | 未确认命令持久化并以相同 commandId 重发 | 必须 | - | 断网复核 |
| 恢复 | 断线重连并恢复全量私有快照；不验收未实现的 Bot 托管 | 必须 | 可操作 | 双设备 |
| 房间 | 建房、入房、准备、Bot、仅房主开局 | 必须 | 可操作 | 双设备 |
| 教程 | 三步说明依次呈现；最后一步启动 `room.tutorial=true` 的权威 Bot 教学局并显示渐进提示 | 必须 | 截图 + 可操作 | 单设备完整一局 |
| 对局 | 服务器绑定 actor、服务器时钟、私有投影 | 必须 | 可操作 | 双设备 |
| 定时 | stale/early deadline 无害，进程重启后到期任务可恢复 | 必须 | - | 故障演练 |
| 小游戏 | 工程可被微信开发者工具导入并编译 | 构建 | 必须 | - |
| UI | 25 个场景均可从 scene registry 呈现 | 必须 | 截图 | 抽查 |
| UI | 390 x 844、安全区、DPR、滚动、命中无偏移 | 布局测试 | 截图 | iOS + Android |
| 平台 | 登录、SocketTask、存储、键盘、音频、振动、分享 | fake adapter | 调试 | iOS + Android |
| 资源 | 主包预算、图片清单、禁止 Web 字体进入产物 | 必须 | 构建 | - |
| 启动安全 | 缺失云环境配置显示失败；生产 query 无法启用 Demo、开发认证或改写服务地址；基础库最低版本为 `2.23.0` | 必须 | 调试 | 抽查 |
| 运维 | 单实例健康检查、优雅关闭、结构化脱敏日志；公网关闭时 `callContainer`/`connectContainer` 可用 | 必须 | - | 部署演练 |
| 持久化 | `rooms.tutorial` 迁移、读写与重启后教学局恢复 | 静态路径 | - | 真实 MySQL 演练 |
| 合规 | AppID、云托管环境授权、备案、实名/防沉迷、隐私和审核 | - | - | 所有者 |

## 自动化完成条件

```powershell
npm ci
npm run typecheck
npm test
npm run build
git diff --check
```

## 微信工具完成条件

构建完成后，以 `apps/minigame` 文档给出的路径执行微信开发者工具 CLI 导入。测试 AppID 只能证明工程结构和运行时兼容；真实 `callContainer` 登录、`connectContainer` 实时连接、分享和跨设备 Socket 必须使用正式 AppID 与已授权云托管环境复核。生产验收时保持服务公网访问关闭。

当前机器尚未取得微信开发者工具或真机截图证据，因此这些列没有被标记为通过；详情见 `apps/minigame/design-qa.md`。

## 外部环境完成条件

云托管 MySQL 已通过就绪检查；迁移回滚、备份恢复、故障注入与实例重启仍须在部署数据库上演练。正式验收还需要项目所有者提供正式 AppID、已授权微信云托管环境、生产数据库、备案及合规配置。微信云托管可信身份模式不需要把 AppSecret 放入客户端或服务配置，也无需通讯域名配置。自动测试通过不能替代这些外部证据。

## 视觉完成条件

`prototype/audit/current/after/` 的 25 张 390 x 844 截图仅是设计基准。小游戏截图必须来自微信开发者工具或真机，再与同状态基准组合比较；所有 P0/P1/P2 修复后才能将小游戏视觉 QA 标记为通过。无法捕获真实小游戏截图时必须标为 blocked，不能复用 Web 截图冒充证据；当前证据状态见 `apps/minigame/design-qa.md`。
